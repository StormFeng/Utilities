<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nof.utilities">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.nof.utilities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.HttpActivity" />
        <activity android:name=".activity.FadeActivity" />
        <activity android:name=".fragment.ActivityFragment" />
        <activity android:name=".activity.AnimActivity" />
        <activity android:name=".fragment.DetailActivity" />
        <activity android:name=".activity.StarsMenuActivity" />
        <activity android:name=".activity.BezierActivity" />
    </application>

</manifest>